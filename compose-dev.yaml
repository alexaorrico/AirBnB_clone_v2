services:
  app:
    entrypoint:
    - sleep
    - infinity
    image: ubuntu
    build: 
      context: ./.devcontainer
      dockerfile: Dockerfile
    init: true
    volumes:
    - type: bind
      source: /var/run/docker.sock
      target: /var/run/docker.sock
