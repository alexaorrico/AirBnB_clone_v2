state = storage.get("State", str(state_id))
    if not state:
        abort(404)
    storage.delete(state)
    storage.save()
    return jsonify({}), 200
 city = storage.get("City", city_id)
    if city:
        sto.delete()
        storage.save()
        return jsonify({}), 200
    else:
        abort(404)

