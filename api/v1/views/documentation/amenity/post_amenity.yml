This Links an amenity object to a place specified
    ---
    tags:
      - Amenities
    parameters:
      - name: amenity_id
        in: body
        required: true
        requires:
          - name
        properties:
          name:
            type: string

    responses:
      400:
        description: Not a JSON
      201:
        description: request was executed successfully
