This  Updates the state information using its specified ID
    ---
    tags:
      - States
    parameters:
      - name: state_id
        in: path
        type: string
        required: true
        description: the unique id of the state
      - name: request
        in: body
        required: true

    responses:
      404:
        description: State not found
      200:
        description: Request successfully completed
