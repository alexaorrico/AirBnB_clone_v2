    Updates the state info based on ID
    ---
    tags:
      - States
    parameters:
      - name: state_id
        in: path
        type: string
        required: true
        description: the unique id of the state
    responses:
      404:
        description: State not found
      200:
        description: A language with its awesomeness
