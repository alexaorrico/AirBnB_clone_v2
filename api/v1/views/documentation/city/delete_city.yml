This deletes a city using its id
    ---
    tags:
      - Cities
    parameters:
      - name: city_id
        in: path
        type: string
        required: true
        description: the id of the city
    responses:
      404:
        description: City not found!
      200:
        description: Request was completed successfully
