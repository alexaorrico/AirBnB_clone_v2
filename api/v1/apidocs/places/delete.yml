Deletes a place.
---
tags:
  -  "places"
produces:
  - "application/json"
parameters:
  - in: path
    name: place_id
    description: "The ID of the place to delete."
    required: true
    schema:
      type: "string"
responses:
  200:
    description: "An empty object representing the deleted place."
    schema:
      type: "object"
  404:
    description: "Not found"
